services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: dev-toolbox
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
