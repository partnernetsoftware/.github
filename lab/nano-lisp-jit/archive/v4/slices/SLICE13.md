# v4 slice-13 — emit manifest（scoped）

**前置**：[`SLICE12.md`](SLICE12.md)。

## 交付

- 构建日志 `aarch64.emit.manifest=add-exit-v1`
- `bootstrap-v4-slice13-emit-manifest.lisp` 证据

## 签收

编排轨 S7：`bootstrap-v4-squad-s7-signal.lisp`（并行样本）。
