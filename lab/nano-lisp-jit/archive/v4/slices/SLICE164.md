# v4 wave164 — emit-close-b

长程自主 · [`EVAL.md`](EVAL.md) · [`LONG-RUN-TODO.md`](LONG-RUN-TODO.md)。

§洋葱
- 外层目标：emit-close-b 收紧 emit 扩散后的尾部证据。
- close 层优先检查前几波 emit 样本是否仍可批量通过。
- 新增样本只补缺口，不扩张到新的 C 语义。
- catalog 与 wave index 要让 slice164 的 close 意图可见。
- emit-close tick 需要给出可对照的最终输出片段。
- 以无回归、无孤儿样本、无新增 FAIL 完成本层封口。
