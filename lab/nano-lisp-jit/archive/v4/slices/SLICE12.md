# v4 slice-12 — IR 表 v3（movz 基址表）

**前置**：[`SLICE11.md`](SLICE11.md)。

## 交付

- `a64_ir_movz_base_v3` + `a64_movz_from_table_v3`
- 日志 `aarch64.emit.ir.table.version=v3`
- add17（9+8）回归

## 签收

`v4-slice12-scoped` 门禁子集（并入 `v4-complete-scoped`）。
