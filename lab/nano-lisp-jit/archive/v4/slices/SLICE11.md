# v4 slice-11 — IR 表 v2 固定字发射（scoped）

**前置**：[`SLICE10.md`](SLICE10.md)。

## 交付

| 轨 | 内容 |
|----|------|
| **codegen** | `a64_ir_fixed_word_v2` 发射 ADD/SVC/MOVZ_X8；日志 `ir.table.version=v2` |
| **编排** | `bootstrap-v4-squad-s6-dispatch.lisp` |

## 签收

`signoff.id=v4-slice11-scoped`
