# v5 决策（物理轨）

**前提**：v4.5 规划面已签收（`/goal` 26/26 · 扩展活图 Wave34–51 · `v45_terminal_complete.100`）。

## tier0 — 诚实边界

- **禁止**把 Wave52 维护签收写成 154KB 物理 DONE 或全 monorepo zero_c。
- v5 物理轨独立 SSOT；与 v4.5 扩展活图 **不混键**。

## tier1 — 首批物理目标

1. 154KB `lispjit.c` → Lisp codegen 扩面（gen 链延续 v4 runner-codegen）
2. `nano-lisp.com` 产品硬切（host 仍可 dual-run 过渡期）
3. 外层 CI 逐步 plan-only（`.sh` 仅 host 壳）

## 证据

维护轨：`v45.v45.v5_open_maintenance_continue.100=1`  
物理轨：（待 v5 Wave1 定义）
