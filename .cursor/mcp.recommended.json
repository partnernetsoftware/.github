{
  "_note": "Canonical copy: workers/mcp-cf-bots/mcp.recommended.json",
  "_agentGuide": "INDEX workers/mcp-cf-bots/INDEX.md § Agent（MCP）; server sends initialize.instructions",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    },
    "mcp-cf-bots": {
      "url": "${env:MCP_CF_BOTS_URL}/mcp",
      "headers": {
        "Authorization": "Bearer ${env:MCP_CF_BOTS_TOKEN}"
      }
    }
  }
}
