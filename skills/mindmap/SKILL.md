---
description: .mindmap 可执行树格式
---

# mindmap

- `mindmap.mindmap` — 自举规范（语法/节点/状态/执行语义），人类可读。
- `mindmap.ebnf` — 机器可读 EBNF（v0.2 同源），用于解析器实现与校验。
- `ARCHITECTURE.mindmap` — `.mindmap` 架构边界（含已弃用的 `.sd` Sardine 历史 DSL 归档说明）。

下游：`skills/onion-tdd-workflow/` 基于此构建 TDD 工作流闭环。
