### mindmap v0.2
// .mindmap 可执行树格式 — 为 LLM 设计的上下文格式
// 设计目标: 比 markdown 更省 token / 更少歧义 / 可执行

- {D} 设计原理 — 为什么不用 Markdown
  - Markdown 问题: heading 层级歧义、列表与段落边界模糊、无原生状态标记、纯线性无控制流
  - .mindmap 优势: 缩进=无歧义层级 | [→][✓]原生态状态 | [seq]/[if]/[while] 原生控制流
  - LLM 视角: 树结构天然匹配 transformer 注意力机制；同信息量 token 节省 30-50%
  - AI 时代新发明: 非替代 markdown，而是补充——markdown 用于人类阅读，.mindmap 用于 LLM 消费与执行

- {O} 输出契约（生产侧 agent 行为约束）
  - 唯一输出 = 完整或增量 .mindmap 文本，禁止前后散文/寒暄/总结
  - 禁止"我刚做了 X"/"接下来计划 Y" 这类历史叙述——状态机 [~]→[✓] 与 `// 注释`已是历史
  - 禁止重述输入树——增量改动直接给 diff 或新树
  - 思考过程不外泄：所有推理沉淀进节点 summary / `- md:` / `// 注释`，不写"思考链"段落
  - 不确定时用 `[ ]` 待办 + `// TODO: 原因` 表达，不要用自然语言提问
  - 元信息（耗时/token/版本）一律不输出，调度器自有遥测
  - 增量约定: 只输出受影响节点的最小子树，根需带 `{id}` 锚定父级以便定位合并

- {S} 语法
  - 缩进决定层级（推荐2空格/tab，解析器对奇数空格宽容降级不拒绝）
  - `- {id} 标题 [status] <extensions>` = 节点定义
  - `// 注释` = 注释行
  - `- [type] 标题` = 逻辑节点（seq/if/while）
  - 子节点行内字段: `- key: value`
  - `---` 分隔多树（单行三个减号）
  - extensions 见 {F}（#tag / @k=v / ->ref，空格分隔，尾随节点标题后）

- {N} 节点类型
  - 普通节点: 无前缀 | 例: `- {M} 模块 [→]`
  - [seq] 顺序块: 子节点按序执行，全[✓]→父[✓]
  - [if] 分支块: [0]=条件 [1]=then [2]=else
  - [while] 循环块: [0]=条件 [1]=循环体 | maxIterations=10
  - // 注释行: 忽略

- {M} 内嵌 Markdown — 节点内支持 markdown 片段
  - 行内: `- md: **粗体** 和 [链接](url)`
  - 多行块: 子节点 `- md:` 后跟缩进段落（每行额外缩进一层）
  - 用途: 节点说明、代码块、富文本描述——树提供结构，markdown 提供内容
  - 例:
    ```
    - {N} 算法说明
      - md: |
        时间复杂度 **O(n log n)**，空间 O(1)。
        参考 [Wikipedia](https://en.wikipedia.org/wiki/Quicksort)
        - 分治策略
        - 原地排序
    ```

- {Z} 状态标记
  - [ ] 待处理
  - [~] 进行中
  - [P] 暂停
  - [D] 委派中
  - [→] 就绪/可执行
  - [✓] 完成
  - [✗] 失败/阻塞
  - 合法迁移: `[ ]→[→]→[~]→{[✓]|[✗]}` | `[~]↔[P]` | `[→]→[D]→[~]` | `[✗]→[ ]`（重试）

- {E} 执行语义
  - 调度器扫描所有[→]叶子 → 派发agent执行 → 完成[✓]回溯父节点
  - [seq]: 子顺序推进 → 全[✓]→父[✓]
  - [if]: 执行条件 → 选then/else → 分支[✓]→父[✓]
  - [while]: 条件真→执行体→重回条件 | 假→父[✓]
  - 逻辑节点自身走[ ]→[→]→[~]→[✓]
  - 幂等约定: `[→]` 节点应设计为可重入，调度器允许重试；有副作用且不可重入的节点必须 `@once=true` 标记

- {F} 字段约定（可选扩展）
  - 通用: 节点可携带任意 `- key: value` 行内子字段
  - `- md:` 保留字段: 内嵌 markdown 片段
  - 行尾 extensions（v0.2，空格分隔，可混合）:
    - `#tag` — 标签 | 例: `- {T} 任务 [→] #urgent #wip`
    - `@key=value` — 属性 | 例: `- {T} 任务 @owner=wjc @prio=P0`
    - `@key="带空格的值"` — 值含空格用双引号
    - `->id` — 软引用/连线 | 例: `- {T} 任务 ->M` 指向 {M} 节点
  - 解析顺序: 节点行 = `- {id} summary... [status] ext1 ext2 ...`，extensions 从行尾向前贪婪匹配（# / @ / -> 前缀）
  - 保留属性（runtime 语义，parser 必须保留透传）:
    - `@iter=N` — [while] 体上的当前迭代计数（1-based）
    - `@maxIter=N` — [while] 节点覆盖默认 10
    - `@branch=then|else` — [if] 上一次求值选中的分支
    - `@once=true` — 节点有副作用不可重入，禁止重试
    - `@owner=name` / `@prio=P0..P3` — 调度器路由/排序软提示
  - 解析容错（规范承诺）: 缩进奇数空格、tab/space 混用、行尾多余空格、未识别 ext → 一律降级保留，不拒绝

- {X} 嵌入约定
  - 纯文本: 文件后缀 .mindmap，首行 `### mindmap v0.2`

- {B} 自举声明 [✓]
  - 本文件即用 .mindmap 定义 .mindmap 自身
  - 理论完备: 树+控制流+状态机 可表达任意结构化程序
  - 定位: AI 时代原生上下文格式 — 为 LLM 设计，人类可读
  - 配套机器可读规范: `mindmap.ebnf`（v0.2 同源）

