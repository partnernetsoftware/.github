# mcp-cf-bots

Cloudflare HTTP MCP + REST（`sess_*` 会话、`mem_*` 记忆、`cfb_*` 多租户）。

**文档 SSOT → [INDEX.md](INDEX.md)** · 思维树 → [mcp-cf-bots.mindmap](mcp-cf-bots.mindmap)

```bash
cd workers/mcp-cf-bots && ./scripts/deploy.sh
```
