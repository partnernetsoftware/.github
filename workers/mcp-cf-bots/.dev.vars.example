# Copy to .dev.vars for `wrangler dev` (never commit .dev.vars)
VAULT_TOKEN=dev-token-change-me
# MCP_SESSION_SECRET=optional-separate-from-vault-for-mcp-session-id
DEFAULT_OWNER=local-dev
DEFAULT_SESSION_SOURCE=browser-use
# Optional W4: Vectorize list/GC + status page analytics
# CF_ACCOUNT_ID=
# CF_API_TOKEN=
