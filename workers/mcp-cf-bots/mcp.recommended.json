{
  "_agentGuide": "Agents: read workers/mcp-cf-bots/INDEX.md § Agent（MCP）. Server returns initialize.instructions; use mem_* for user memory, sess_* after browser login.",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    },
    "mcp-cf-bots": {
      "url": "${env:MCP_CF_BOTS_URL}/mcp",
      "headers": {
        "Authorization": "Bearer ${env:MCP_CF_BOTS_TOKEN}"
      }
    }
  }
}
